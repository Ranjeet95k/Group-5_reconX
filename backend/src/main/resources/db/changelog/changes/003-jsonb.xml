<?xml version="1.0" encoding="UTF-8"?>
<!--
 ============================================================================
 TICKET-ADV009 / ADV014 — JSONB column on instruments (+ GIN index on Postgres)
 WHAT:    instruments.metadata as JSONB to hold per-asset-class detail.
 HOW:     Postgres-only step guarded by dbms precondition.
 WHY:     H2 does not support JSONB. MARK_RAN records this step as handled
          when running against non-Postgres databases.
 OBSERVE: Postgres creates metadata JSONB column and GIN index.
 ============================================================================
-->

<databaseChangeLog
        xmlns="http://www.liquibase.org/xml/ns/dbchangelog"
        xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
        xsi:schemaLocation="http://www.liquibase.org/xml/ns/dbchangelog
                            http://www.liquibase.org/xml/ns/dbchangelog/dbchangelog-4.27.xsd">


    <changeSet id="003-jsonb-on-instruments" author="trainer">

        <!-- TICKET-ADV014: JSONB exists only in PostgreSQL. Skip safely on H2/dev databases. -->
        <preConditions onFail="MARK_RAN">
            <dbms type="postgresql"/>
        </preConditions>


        <sql>
            ALTER TABLE instruments 
            ADD COLUMN metadata JSONB DEFAULT '{}'::JSONB NOT NULL;
        </sql>


        <sql>
            CREATE INDEX idx_instruments_metadata_gin
            ON instruments 
            USING GIN (metadata jsonb_path_ops);
        </sql>

        <sql>ALTER TABLE instruments ADD COLUMN metadata JSONB DEFAULT '{}'::JSONB NOT NULL;</sql>
        <sql>CREATE INDEX idx_instruments_metadata_gin ON instruments USING GIN (metadata jsonb_path_ops);</sql>

        <rollback>
            <sql>
                DROP INDEX IF EXISTS idx_instruments_metadata_gin;
            </sql>

            <sql>
                ALTER TABLE instruments DROP COLUMN metadata;
            </sql>
        </rollback>


    </changeSet>


</databaseChangeLog>